# DL3008 (pin apt-get install versions) — намеренно игнорируем.
# Пиннинг дал бы воспроизводимость, но лишил бы автоматических
# security-патчей при пересборке (образ и так пересобирается в CI
# на каждый пуш, а Trivy-скан в той же джобе ловит известные CVE).
ignored:
  - DL3008
  - DL3008
  - DL3062
